---
// Componente Contacto
---

<section id="contacto" class="py-20 bg-gradient-to-b from-slate-900 to-slate-800">
  <div class="mx-auto max-w-7xl px-6">
    <div class="text-center mb-14">
      <h2 class="text-4xl font-bold tracking-tight text-white">¿Trabajamos juntos?</h2>
      <p class="mt-3 text-slate-300 max-w-2xl mx-auto">Cuéntame sobre tu proyecto y te responderé en menos de 24 horas.</p>
    </div>

    <div class="grid lg:grid-cols-3 gap-8">
      <!-- Info quick cards -->
      <div class="lg:col-span-1 grid sm:grid-cols-3 lg:grid-cols-1 gap-6">
        <div class="bg-white/5 backdrop-blur-sm rounded-xl p-5 border border-white/10 text-center">
          <div class="w-12 h-12 mx-auto bg-white/10 rounded-lg flex items-center justify-center mb-3">
            <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
          </div>
          <h3 class="text-white font-semibold">Email</h3>
          <a href="mailto:ibarrabelloalisha@gmail.com" class="text-slate-300 hover:text-white text-sm">ibarrabelloalisha@gmail.com</a>
        </div>
        <div class="bg-white/5 backdrop-blur-sm rounded-xl p-5 border border-white/10 text-center">
          <div class="w-12 h-12 mx-auto bg-white/10 rounded-lg flex items-center justify-center mb-3">
            <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
          </div>
          <h3 class="text-white font-semibold">Teléfono</h3>
          <a href="tel:+34692616005" class="text-slate-300 hover:text-white text-sm">+34 692 616 005</a>
        </div>
        <div class="bg-white/5 backdrop-blur-sm rounded-xl p-5 border border-white/10 text-center">
          <div class="w-12 h-12 mx-auto bg-white/10 rounded-lg flex items-center justify-center mb-3">
            <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
          </div>
          <h3 class="text-white font-semibold">Ubicación</h3>
          <span class="text-slate-300 text-sm">Madrid, España</span>
        </div>
      </div>

      <!-- Quick brief form -->
      <div class="lg:col-span-2">
        <div class="bg-white/5 backdrop-blur-sm rounded-2xl p-8 border border-white/10">
          <h3 class="text-2xl font-bold tracking-tight text-white">Cuéntame sobre tu proyecto</h3>
          <p class="text-slate-300 mt-2">Completa este breve formulario y se abrirá tu cliente de correo con el mensaje preparado.</p>

          <form id="contact-form" class="mt-6 grid md:grid-cols-2 gap-4">
            <div class="col-span-2 md:col-span-1">
              <label for="name" class="block text-sm text-slate-300 mb-1">Nombre</label>
              <input id="name" name="name" type="text" required class="w-full rounded-lg bg-white/10 border border-white/20 px-3 py-2 text-white placeholder-slate-400 focus-ring" placeholder="Tu nombre" />
            </div>
            <div class="col-span-2 md:col-span-1">
              <label for="email" class="block text-sm text-slate-300 mb-1">Email</label>
              <input id="email" name="email" type="email" required class="w-full rounded-lg bg-white/10 border border-white/20 px-3 py-2 text-white placeholder-slate-400 focus-ring" placeholder="tucorreo@ejemplo.com" />
            </div>
            <div class="col-span-2">
              <label for="subject" class="block text-sm text-slate-300 mb-1">Asunto</label>
              <input id="subject" name="subject" type="text" class="w-full rounded-lg bg-white/10 border border-white/20 px-3 py-2 text-white placeholder-slate-400 focus-ring" placeholder="Rediseño app móvil, landing, design system..." />
            </div>
            <div class="col-span-2">
              <label for="message" class="block text-sm text-slate-300 mb-1">Mensaje</label>
              <textarea id="message" name="message" rows="5" required class="w-full rounded-lg bg-white/10 border border-white/20 px-3 py-2 text-white placeholder-slate-400 focus-ring" placeholder="Breve descripción, objetivos y plazo ideal"></textarea>
            </div>
            <div class="col-span-2 flex flex-wrap gap-2">
              <span class="tag bg-white/10 border-white/20 text-slate-200">App móvil</span>
              <span class="tag bg-white/10 border-white/20 text-slate-200">Web</span>
              <span class="tag bg-white/10 border-white/20 text-slate-200">Design System</span>
              <span class="tag bg-white/10 border-white/20 text-slate-200">Prototipado</span>
              <span class="tag bg-white/10 border-white/20 text-slate-200">Research</span>
            </div>
            <div class="col-span-2 mt-2">
              <button type="submit" class="w-full md:w-auto bg-white text-slate-900 font-semibold px-6 py-3 rounded-xl hover:bg-slate-100 transition-colors shadow">
                💌 Preparar email
              </button>
            </div>
          </form>

          <div class="flex items-center gap-2 text-slate-400 text-sm mt-6">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <span>Respuesta en menos de 24 horas</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('contact-form');
    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      const name = (document.getElementById('name') as HTMLInputElement)?.value.trim();
      const email = (document.getElementById('email') as HTMLInputElement)?.value.trim();
      const subject = (document.getElementById('subject') as HTMLInputElement)?.value.trim() || 'Contacto';
      const message = (document.getElementById('message') as HTMLTextAreaElement)?.value.trim();
      const to = 'ibarrabelloalisha@gmail.com';
      const body = `Hola Alisha,%0D%0A%0D%0ASoy ${encodeURIComponent(name)} (${encodeURIComponent(email)}).%0D%0A%0D%0A${encodeURIComponent(message)}%0D%0A%0D%0AGracias!`;
      const mailto = `mailto:${to}?subject=${encodeURIComponent(subject)}&body=${body}`;
      window.location.href = mailto;
    });
  </script>
</section>

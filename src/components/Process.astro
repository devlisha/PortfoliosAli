---
// UX Process Section with Clear Steps - Brutalist Design
---

<div class="brutalist-process">
  <!-- Main title block -->
  <div class="brutalist-box mb-8" style="background: var(--black); color: var(--white); padding: 2rem;">
    <h2 class="brutalist-heading">PROCESS</h2>
    <p class="font-bold text-lg mt-4 uppercase">FROM PROBLEM UNDERSTANDING TO DEVELOPMENT TEAM HANDOFF</p>
  </div>

  <!-- Process steps grid -->
  <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
    <!-- Step 1: Research -->
    <div class="brutalist-box" style="background: var(--red); color: var(--white); padding: 2rem;">
      <div class="flex items-center justify-center w-12 h-12 bg-black text-white font-black text-xl mb-4">
        01
      </div>
      <h3 class="font-black uppercase text-xl mb-4 tracking-tight">RESEARCH</h3>
      <p class="font-bold text-sm uppercase leading-tight">
        INTERVIEWS, BENCHMARKS, ANALYTICS AND HYPOTHESIS DEFINITION
      </p>
    </div>

    <!-- Step 2: Ideation -->
    <div class="brutalist-box" style="background: var(--yellow); color: var(--black); padding: 2rem;">
      <div class="flex items-center justify-center w-12 h-12 bg-black text-white font-black text-xl mb-4">
        02
      </div>
      <h3 class="font-black uppercase text-xl mb-4 tracking-tight">IDEATION</h3>
      <p class="font-bold text-sm uppercase leading-tight">
        FLOW MAPS, WIREFRAMES AND SOLUTION SELECTION
      </p>
    </div>

    <!-- Step 3: Prototyping -->
    <div class="brutalist-box" style="background: var(--blue); color: var(--white); padding: 2rem;">
      <div class="flex items-center justify-center w-12 h-12 bg-black text-white font-black text-xl mb-4">
        03
      </div>
      <h3 class="font-black uppercase text-xl mb-4 tracking-tight">PROTOTYPING</h3>
      <p class="font-bold text-sm uppercase leading-tight">
        MEDIUM/HIGH FIDELITY PROTOTYPES AND MICROINTERACTIONS
      </p>
    </div>

    <!-- Step 4: Validation -->
    <div class="brutalist-box" style="background: var(--green); color: var(--black); padding: 2rem;">
      <div class="flex items-center justify-center w-12 h-12 bg-black text-white font-black text-xl mb-4">
        04
      </div>
      <h3 class="font-black uppercase text-xl mb-4 tracking-tight">VALIDATION</h3>
      <p class="font-bold text-sm uppercase leading-tight">
        USER TESTING, ITERATION AND HANDOFF WITH DOCUMENTATION
      </p>
    </div>
  </div>

  <!-- Bottom accent block -->
  <div class="brutalist-box mt-8" style="background: var(--black); color: var(--white); padding: 1rem;">
    <p class="font-black uppercase text-center text-lg">
      DATA-DRIVEN DESIGN • CONSTANT ITERATION • CLOSE COLLABORATION
    </p>
  </div>
</div>

<style>
.brutalist-process {
  /* Additional styling for process section */
}

.brutalist-heading {
  font-family: 'Arial Black', Arial, sans-serif;
  font-size: 3rem;
  font-weight: 900;
  letter-spacing: -0.02em;
  margin: 0;
}

@media (max-width: 768px) {
  .brutalist-heading {
    font-size: 2rem;
  }
  
  .grid {
    grid-template-columns: 1fr;
  }
}
</style>
